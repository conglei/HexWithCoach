---
"hex-app": minor
---

iOS keyboard: adopt KeyboardKit for a system-matching key layout (native colors, per-device sizing, dark mode, and key magnification) with the Hex dictation strip above the keys. Replaces the hand-built QWERTY clone, which couldn't match the system keyboard's appearance.
