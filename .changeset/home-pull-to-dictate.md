---
"hex-app": minor
---

Home page polish: the layout now fills the screen so the Recent list sits flush above the tab bar (the record mic moved lower, with the empty space above it). Adds a second way to start a note — pull the Home screen down past the threshold and release to start dictating, with a growing gradient mic indicator ("Pull to dictate" → "Release to dictate"), a haptic when it arms, and a smooth animation. Tapping the mic still works exactly as before.
