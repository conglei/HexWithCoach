---
"hex-app": patch
---

Redesign macOS Settings to the HIG-standard pattern: a dedicated, tabbed Settings window (icon tabs for General, Recording, Models, Coach, Sync & Privacy, Transforms, About) opened via the menu / ⌘,, instead of a section nested in the main window. Adds an iCloud sync toggle with live account status and a sideloaded pronunciation-model status row (MC-R11).
