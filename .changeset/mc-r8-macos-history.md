---
"hex-app": patch
---

macOS History: Notes|Dictation segment with windowed, searchable browse (MC-R8). The companion-window History tab now mirrors the iOS surface — a Notes/Dictation segmented control over a date-scoped, paginated fetch (This week / This month / All + Load older) with search across all scopes. Opening a transcript shows the detail with audio playback, copy, pronunciation summary, and a cascade delete. List reads stay lean and never fault the heavy analysis sidecar.
