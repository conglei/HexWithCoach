---
"hex-app": patch
---

Coach cards are now self-explanatory and practiceable: each card shows the flagged words inside their original sentence (so the quote reads in context), "Say it better" speaks and scores a real, natural sentence instead of a phonetic respelling like "VERY [vɛr-ee]", and tapping a card (or its new note button) opens the source note with the flagged span highlighted — even when there's no audio.
