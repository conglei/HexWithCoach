---
"hex-app": patch
---

Keyboard: restyle the dictation toolbar to match Voco's brand. The "Tap to dictate" pill now uses the app's blue→violet gradient (instead of flat blue) with a capsule shape and a soft glow; recording uses a clean red→pink instead of the muddy brick; the settings and Cancel buttons are lighter and quieter so the pill stays the focus (#89).
