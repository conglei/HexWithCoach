---
"hex-app": minor
---

UI refresh (1/n): introduce the shared HexTheme design system — a blue→purple brand gradient, a white rounded-card surface, a gradient primary button, and a gradient mic control — and restyle the Home and Recording screens to match the new mockups. Home now leads with a greeting + bold "Hex" title, a dictation status pill, a big gradient mic ("Tap to dictate"), and Recent cards (with "See all" → History). Recording gets a Cancel · timer · language top bar, a LISTENING state with a gradient waveform, and a gradient stop button.
