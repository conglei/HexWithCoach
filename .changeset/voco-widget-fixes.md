---
"hex-app": patch
---

Polish the iOS widgets: the Flow Session Live Activity now shows the Voco logo and name on the Lock Screen (so you can tell which app the banner is from) and uses a smaller End button. The Home Screen widget is split into two controls — tapping the mic starts recording a new note in the app, while tapping the keyboard status pill jumps to Settings to turn the keyboard on or off (#86).
