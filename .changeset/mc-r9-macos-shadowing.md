---
"hex-app": patch
---

Make the macOS Practice session a closed shadowing loop (MC-R9): hear the native rewrite via on-device TTS, record your attempt, transcribe it on-device, and score the match — persisting per-segment scores (and GOP deltas when a phoneme model is present) on the shared PracticeItem.
