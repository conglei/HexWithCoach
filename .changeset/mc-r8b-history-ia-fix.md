---
"hex-app": patch
---

macOS: remove the duplicate History tab from the legacy settings window (History is now the top-level companion-window section), and default macOS History to the Dictation segment so the Mac's primary capture is visible.
