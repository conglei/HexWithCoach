---
"hex-app": patch
---

Persist macOS transcription history through the shared SwiftData store so it syncs via CloudKit, built on the lean TranscriptEntry row + faulted analysis sidecar with a windowed History fetch (MC-R3).
