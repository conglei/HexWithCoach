---
"hex-app": patch
---

iOS UX fixes: pull-to-dictate on Home now triggers (the screen couldn't overscroll before); the Review tab disables its review button and shows a clear "Reviewing…" banner while analyzing instead of an easy-to-miss spinner; and History shows each transcript's type as an icon instead of a text label.
