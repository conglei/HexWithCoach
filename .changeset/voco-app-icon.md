---
"hex-app": patch
---

Use a single Icon Composer `voco.icon` as the app icon for both the iOS (Voco) and macOS (VocoMac) targets, and remove the now-unused icon assets (old AppIcon sets + stray imagesets).
